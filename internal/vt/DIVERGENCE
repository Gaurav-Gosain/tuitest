# Files that deliberately differ from the upstream commit in UPSTREAM, one per
# line. Every entry carries a fix made here that tuios does not have yet, or a
# test that pins one; VENDOR.md says what each change is. Port them to tuios,
# re-sync, and delete the line. scripts/vendor-vt.sh merges upstream changes
# into a listed file instead of overwriting it. The drift check requires a
# listed file to actually differ, so a stale entry fails the build rather than
# rotting quietly.
csi_mode.go
csi_sgr.go
handlers.go
mode.go
conform_erase_test.go
conform_screen_test.go
csi_sgr_rgb_test.go
